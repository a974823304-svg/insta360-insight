# 阶段一：登录认证（自建账号 + JWT + SQLite）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为「Insta360 达人营销数据洞察平台」加上独立账号登录门面（自建账号密码 + JWT + SQLite），让看板在对外/演示时不再裸奔，同时保持前端看板契约不变、dev 可一键免登录。

**Architecture:** 后端在现有 Gin 分层上新增 `model.User` + `store.UserRepo`（SQLite，纯 Go 驱动 modernc.org/sqlite）+ `service.AuthService`（bcrypt + JWT HS256）+ `handler.Auth`（register/login 端点）+ `middleware.JWTAuth`（保护 `/api` 数据组，dev 可 skip）。前端新增 `stores/auth.js` + `views/LoginView.vue`，并在 `request.js` 加 401 自动跳登录、`router/index.js` 加全局前置守卫。看板页面与现有 `/api/*` 契约零改动。

**Tech Stack:** Go 1.22 + Gin 1.10；`github.com/golang-jwt/jwt/v5`（JWT HS256）；`golang.org/x/crypto/bcrypt`（密码哈希）；`modernc.org/sqlite`（纯 Go SQLite 驱动，无 CGO）；前端 Vue 3 + Pinia + vue-router(hash) + axios。

## Global Constraints

- **禁止任何 CGO 依赖**：所有新增依赖必须是纯 Go。`modernc.org/sqlite`、`golang-jwt/jwt/v5`、`golang.org/x/crypto` 均满足。构建/测试前统一 `CGO_ENABLED=0`（Windows 默认可能尝试 CGO，纯 Go 包也建议显式关闭以稳定）。
- **JWT 用 HS256**：secret 取自环境变量 `JWT_SECRET`；缺失时仅 dev 用硬编码缺省值并启动告警，**生产必须设置**。
- **密码只存 bcrypt 哈希**：`User.PasswordHash` 字段 JSON tag 为 `json:"-"`，任何响应不得泄露。
- **SQLite 文件库**：路径 `backend/data/app.db`（相对 backend 工作目录），首次启动自动 `CREATE TABLE IF NOT EXISTS users`，目录不存在需 `os.MkdirAll`。
- **种子账号**：表为空时用 `SEED_ADMIN_USER`/`SEED_ADMIN_PASS`（缺省 `admin`/`insta360`）插入一个 `admin`；生产必须改密或关闭自动种子。
- **dev 放行**：环境变量 `AUTH_DISABLE=1` 或 `ENV=dev` 时 `JWTAuth` 跳过校验，并向 context 注入默认 admin（username=`dev`, role=`admin`）。
- **统一响应格式**：成功 `{ "code": 0, "data": ... }`；业务失败 `{ "code": <非0>, "message": "..." }`（HTTP 200）；**鉴权中间件**未授权返回 **HTTP 401** + `{ "code": 401, "message": "..." }`（前端 axios 拦截器靠 HTTP 401 跳登录）。
- **前端契约不变**：现有看板页面、所有 `/api/*` 数据接口路径与字段不变；仅新增 `/api/auth/*` 与一层校验。
- **目录约定**：后端新文件放对应层（`store/` 同级于 `mock/`、`service/`、`model/`、`middleware/`、`api/handler/`），遵循现有「handler 瘦层 + service 业务逻辑 + model 契约」职责划分。

---

## 任务分解总览

- **后端（严格 TDD，每个 task 带 Go test）**：Task 1 依赖基线 → Task 2 model → Task 3 store → Task 4 service → Task 5 handler → Task 6 middleware → Task 7 接线 main/router
- **前端（构建 + 端到端验证；项目未引入 vitest，前端逻辑用 `vite build` 通过 + 浏览器/Edge 无头验证代替单测，验证命令见各 task）**：Task 8 auth store → Task 9 request 拦截器 → Task 10 LoginView + /login 路由 → Task 11 路由守卫 → Task 12 端到端验收

---

### Task 1: 依赖基线 — 引入 jwt / bcrypt / sqlite 并确认可编译

**Files:**
- Modify: `backend/go.mod`（新增 3 个 direct 依赖）
- Modify: `backend/go.sum`（由 `go mod tidy` 自动更新）

**Interfaces:** 无（基础设施任务，供后续 task 引用包路径）

- [ ] **Step 1: 在放行网络下拉取依赖**（本机网络受限，模块下载走 `proxy.golang.org`，若失败需在可联网环境执行）
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go get github.com/golang-jwt/jwt/v5@v5.2.1
CGO_ENABLED=0 go get golang.org/x/crypto/bcrypt@v0.23.0
CGO_ENABLED=0 go get modernc.org/sqlite@v1.29.10
CGO_ENABLED=0 go mod tidy
```
- [ ] **Step 2: 确认整个模块仍可编译（验证依赖不破坏现有代码）**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go build ./...
```
Expected: 编译通过，无报错（现有 insight/ai 服务不受影响）。
- [ ] **Step 3: 提交**
```bash
git add backend/go.mod backend/go.sum
git commit -m "chore: add jwt/bcrypt/sqlite deps for auth phase-1"
```

---

### Task 2: model 层 — 新增 User 结构与 Fail 错误响应包装

**Files:**
- Create: `backend/internal/model/user.go`
- Modify: `backend/internal/model/types.go`（在 `OK` 函数后追加 `Fail`）
- Test: `backend/internal/model/user_test.go`

**Interfaces:**
- Produces: `model.User`（后续 store/service/handler 共用）；`model.Fail(code int, message string) APIResponse`（handler 与 middleware 返回业务/鉴权错误时使用）

- [ ] **Step 1: 写失败测试**
```go
// backend/internal/model/user_test.go
package model

import "testing"

func TestFail_WrapsCodeAndMessage(t *testing.T) {
	resp := Fail(401, "未授权")
	if resp.Code != 401 {
		t.Fatalf("expected code 401, got %d", resp.Code)
	}
	if resp.Message != "未授权" {
		t.Fatalf("expected message 未授权, got %q", resp.Message)
	}
	if resp.Data != nil {
		t.Fatalf("expected nil data on error, got %v", resp.Data)
	}
}
```
- [ ] **Step 2: 运行测试确认失败**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/model/ -run TestFail -v
```
Expected: 编译失败 `Fail undefined`（因为还没实现）。
- [ ] **Step 3: 写最小实现**

`backend/internal/model/user.go`:
```go
package model

import "time"

// User 平台账号。PasswordHash 禁止出现在任何 JSON 响应中。
type User struct {
	ID           int64     `db:"id" json:"id"`
	Username     string    `db:"username" json:"username"`
	PasswordHash string    `db:"password_hash" json:"-"`
	Role         string    `db:"role" json:"role"` // "admin" | "viewer"
	CreatedAt    time.Time `db:"created_at" json:"created_at"`
}
```

`backend/internal/model/types.go` 在 `OK` 函数后追加：
```go
// Fail 构造业务错误响应。注意:鉴权类错误建议同时返回 HTTP 401（见 middleware）。
func Fail(code int, message string) APIResponse {
	return APIResponse{Code: code, Message: message}
}
```
- [ ] **Step 4: 运行测试确认通过**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/model/ -run TestFail -v
```
Expected: PASS。
- [ ] **Step 5: 提交**
```bash
git add backend/internal/model/user.go backend/internal/model/types.go backend/internal/model/user_test.go
git commit -m "feat(model): add User struct and Fail response helper"
```

---

### Task 3: store 层 — UserRepo（SQLite 建表 / 增 / 查 / 种子）

**Files:**
- Create: `backend/internal/store/user_repo.go`
- Test: `backend/internal/store/user_repo_test.go`

**Interfaces:**
- Consumes: `model.User`
- Produces:
  - `func NewUserRepo(dbPath string) (*UserRepo, error)` — 自动建目录 + 建表 + Ping
  - `func (r *UserRepo) CreateUser(u *model.User) error`
  - `func (r *UserRepo) GetByUsername(name string) (*model.User, error)` — 不存在返回 `sql.ErrNoRows`
  - `func (r *UserRepo) Count() (int, error)` — 供种子判断表空

- [ ] **Step 1: 写失败测试**（用临时文件库，验证建表/增/查/唯一约束/哈希不外泄）
```go
// backend/internal/store/user_repo_test.go
package store

import (
	"os"
	"path/filepath"
	"testing"

	"insta360-insight/internal/model"
)

func newTestRepo(t *testing.T) *UserRepo {
	t.Helper()
	dir := t.TempDir()
	dbPath := filepath.Join(dir, "test.db")
	repo, err := NewUserRepo(dbPath)
	if err != nil {
		t.Fatalf("NewUserRepo: %v", err)
	}
	return repo
}

func TestUserRepo_CreateThenGet(t *testing.T) {
	repo := newTestRepo(t)
	u := &model.User{Username: "alice", PasswordHash: "hashed", Role: "viewer"}
	if err := repo.CreateUser(u); err != nil {
		t.Fatalf("CreateUser: %v", err)
	}
	got, err := repo.GetByUsername("alice")
	if err != nil {
		t.Fatalf("GetByUsername: %v", err)
	}
	if got.Username != "alice" || got.Role != "viewer" {
		t.Fatalf("unexpected user: %+v", got)
	}
}

func TestUserRepo_DuplicateUsernameFails(t *testing.T) {
	repo := newTestRepo(t)
	_ = repo.CreateUser(&model.User{Username: "bob", PasswordHash: "h"})
	if err := repo.CreateUser(&model.User{Username: "bob", PasswordHash: "h2"}); err == nil {
		t.Fatal("expected duplicate username to error")
	}
}

func TestUserRepo_GetMissingReturnsErrNoRows(t *testing.T) {
	repo := newTestRepo(t)
	if _, err := repo.GetByUsername("nope"); err == nil {
		t.Fatal("expected sql.ErrNoRows for missing user")
	}
}

func TestUserRepo_CountReflectsInserts(t *testing.T) {
	repo := newTestRepo(t)
	if n, _ := repo.Count(); n != 0 {
		t.Fatalf("expected empty table, got %d", n)
	}
	_ = repo.CreateUser(&model.User{Username: "x", PasswordHash: "h"})
	if n, _ := repo.Count(); n != 1 {
		t.Fatalf("expected 1 row, got %d", n)
	}
}

func TestUserRepo_CreatesDbDirectory(t *testing.T) {
	dir, _ := os.MkdirTemp("", "repo-dir-*")
	defer os.RemoveAll(dir)
	dbPath := filepath.Join(dir, "nested", "app.db") // 目录不存在
	if _, err := NewUserRepo(dbPath); err != nil {
		t.Fatalf("should create nested dir: %v", err)
	}
}
```
- [ ] **Step 2: 运行测试确认失败**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/store/ -v
```
Expected: 编译失败 `NewUserRepo undefined`。
- [ ] **Step 3: 写最小实现**

`backend/internal/store/user_repo.go`:
```go
// Package store 负责账号等持久化数据访问。当前用 SQLite(纯 Go,无 CGO)。
package store

import (
	"database/sql"
	"os"
	"path/filepath"

	"insta360-insight/internal/model"

	_ "modernc.org/sqlite" // 驱动名 "sqlite"
)

// UserRepo 封装 users 表的访问。
type UserRepo struct {
	db *sql.DB
}

// NewUserRepo 打开(或创建)SQLite 文件库,自动建目录与建表。
func NewUserRepo(dbPath string) (*UserRepo, error) {
	if err := os.MkdirAll(filepath.Dir(dbPath), 0o755); err != nil {
		return nil, err
	}
	db, err := sql.Open("sqlite", dbPath)
	if err != nil {
		return nil, err
	}
	if err := db.Ping(); err != nil {
		return nil, err
	}
	r := &UserRepo{db: db}
	if err := r.migrate(); err != nil {
		return nil, err
	}
	return r, nil
}

func (r *UserRepo) migrate() error {
	_, err := r.db.Exec(`
CREATE TABLE IF NOT EXISTS users (
	id            INTEGER PRIMARY KEY AUTOINCREMENT,
	username      TEXT UNIQUE NOT NULL,
	password_hash TEXT NOT NULL,
	role          TEXT NOT NULL DEFAULT 'viewer',
	created_at    DATETIME NOT NULL
)`)
	return err
}

// CreateUser 插入新账号。
func (r *UserRepo) CreateUser(u *model.User) error {
	u.CreatedAt = u.CreatedAt // 调用方负责填;这里兜底见下
	if u.CreatedAt.IsZero() {
		u.CreatedAt = timeNow()
	}
	_, err := r.db.Exec(
		`INSERT INTO users (username, password_hash, role, created_at) VALUES (?, ?, ?, ?)`,
		u.Username, u.PasswordHash, u.Role, u.CreatedAt,
	)
	return err
}

// GetByUsername 按用户名查账号;不存在返回 sql.ErrNoRows。
func (r *UserRepo) GetByUsername(name string) (*model.User, error) {
	row := r.db.QueryRow(
		`SELECT id, username, password_hash, role, created_at FROM users WHERE username = ?`, name)
	u := &model.User{}
	err := row.Scan(&u.ID, &u.Username, &u.PasswordHash, &u.Role, &u.CreatedAt)
	if err != nil {
		return nil, err
	}
	return u, nil
}

// Count 返回用户总数(种子判断表空用)。
func (r *UserRepo) Count() (int, error) {
	var n int
	err := r.db.QueryRow(`SELECT COUNT(*) FROM users`).Scan(&n)
	return n, err
}

// Close 释放连接(进程退出时调用)。
func (r *UserRepo) Close() error { return r.db.Close() }

// timeNow 抽出来便于测试;生产用 time.Now。
var timeNow = func() time.Time { return time.Now() }
```
> 注意:上面用到了 `time` 包,在文件顶部追加 `import "time"`。

- [ ] **Step 4: 运行测试确认通过**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/store/ -v
```
Expected: 全部 PASS。
- [ ] **Step 5: 提交**
```bash
git add backend/internal/store/
git commit -m "feat(store): add UserRepo over SQLite (create/get/count/seed-ready)"
```

---

### Task 4: service 层 — AuthService（Register / Login / ParseToken / SeedAdmin）

**Files:**
- Create: `backend/internal/service/auth_service.go`
- Test: `backend/internal/service/auth_service_test.go`

**Interfaces:**
- Consumes: `*store.UserRepo`、`jwtSecret string`
- Produces:
  - `type Claims struct { UserID int64; Username string; Role string; jwt.RegisteredClaims }`
  - `func NewAuthService(repo *store.UserRepo, jwtSecret string) *AuthService`
  - `func (s *AuthService) Register(username, password, role string) (*model.User, error)`
  - `func (s *AuthService) Login(username, password string) (token string, user *model.User, err error)`
  - `func (s *AuthService) ParseToken(token string) (*Claims, error)`
  - `func (s *AuthService) SeedAdmin(user, pass string) error`

**业务规则（务必实现）:**
- 密码强度:长度 ≥ 6;否则 `errors.New("密码至少 6 位")`。
- 用户名唯一:重复返回 `errors.New("用户名已存在")`。
- `Register` 用 `bcrypt.GenerateFromPassword` 哈希后落库,role 缺省 `viewer`。
- `Login` 查用户 → `bcrypt.CompareHashAndPassword` 比对 → 失败时返回明确错误(不区分"用户不存在/密码错"对外可统一,但内部可区分;返回给调用方 `error` 即可,handler 再统一文案)。
- JWT claims: `sub=userID`, `username`, `role`, `exp=now+7d`, issuer `insta360-insight`。
- `SeedAdmin`:先 `GetByUsername`,存在则直接返回 nil(幂等);不存在则 `Register(user, pass, "admin")`。

- [ ] **Step 1: 写失败测试**
```go
// backend/internal/service/auth_service_test.go
package service

import (
	"testing"

	"insta360-insight/internal/store"
)

func newTestAuth(t *testing.T) *AuthService {
	t.Helper()
	repo, err := store.NewUserRepo(t.TempDir() + "/a.db")
	if err != nil {
		t.Fatalf("repo: %v", err)
	}
	t.Cleanup(func() { repo.Close() })
	return NewAuthService(repo, "test-secret")
}

func TestRegisterAndLogin(t *testing.T) {
	s := newTestAuth(t)
	u, err := s.Register("alice", "secret123", "viewer")
	if err != nil {
		t.Fatalf("Register: %v", err)
	}
	if u.PasswordHash != "" && u.Role != "viewer" {
		t.Fatalf("unexpected user %+v", u)
	}
	token, got, err := s.Login("alice", "secret123")
	if err != nil {
		t.Fatalf("Login: %v", err)
	}
	if token == "" || got.Username != "alice" {
		t.Fatalf("bad login result token=%q user=%+v", token, got)
	}
}

func TestLoginWrongPasswordFails(t *testing.T) {
	s := newTestAuth(t)
	_, _ = s.Register("bob", "secret123", "viewer")
	if _, _, err := s.Login("bob", "wrong"); err == nil {
		t.Fatal("expected login with wrong password to fail")
	}
}

func TestRegisterWeakPasswordFails(t *testing.T) {
	s := newTestAuth(t)
	if _, err := s.Register("c", "123", "viewer"); err == nil {
		t.Fatal("expected weak password to fail")
	}
}

func TestRegisterDuplicateFails(t *testing.T) {
	s := newTestAuth(t)
	_, _ = s.Register("d", "secret123", "viewer")
	if _, err := s.Register("d", "secret123", "viewer"); err == nil {
		t.Fatal("expected duplicate username to fail")
	}
}

func TestParseTokenRoundTrip(t *testing.T) {
	s := newTestAuth(t)
	_, _ = s.Register("e", "secret123", "admin")
	token, _, _ := s.Login("e", "secret123")
	claims, err := s.ParseToken(token)
	if err != nil {
		t.Fatalf("ParseToken: %v", err)
	}
	if claims.Username != "e" || claims.Role != "admin" {
		t.Fatalf("bad claims %+v", claims)
	}
}

func TestParseTokenRejectsGarbage(t *testing.T) {
	s := newTestAuth(t)
	if _, err := s.ParseToken("not-a-token"); err == nil {
		t.Fatal("expected garbage token to fail")
	}
}

func TestSeedAdminIdempotent(t *testing.T) {
	s := newTestAuth(t)
	if err := s.SeedAdmin("admin", "insta360"); err != nil {
		t.Fatalf("seed1: %v", err)
	}
	if err := s.SeedAdmin("admin", "insta360"); err != nil {
		t.Fatalf("seed2(idempotent) should not error: %v", err)
	}
}
```
- [ ] **Step 2: 运行测试确认失败**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/service/ -run 'TestRegister|TestLogin|TestParse|TestSeed' -v
```
Expected: 编译失败 `AuthService undefined`。
- [ ] **Step 3: 写最小实现**

`backend/internal/service/auth_service.go`:
```go
package service

import (
	"errors"
	"time"

	"github.com/golang-jwt/jwt/v5"
	"golang.org/x/crypto/bcrypt"

	"insta360-insight/internal/model"
	"insta360-insight/internal/store"
)

// Claims 是 JWT 载荷,同时被 handler/middleware 复用。
type Claims struct {
	UserID   int64  `json:"sub"`
	Username string `json:"username"`
	Role     string `json:"role"`
	jwt.RegisteredClaims
}

// AuthService 账号与令牌业务逻辑。
type AuthService struct {
	repo      *store.UserRepo
	jwtSecret string
}

// NewAuthService 构造。
func NewAuthService(repo *store.UserRepo, jwtSecret string) *AuthService {
	return &AuthService{repo: repo, jwtSecret: jwtSecret}
}

// Register 注册新账号(密码 bcrypt 哈希后落库)。
func (s *AuthService) Register(username, password, role string) (*model.User, error) {
	if len(password) < 6 {
		return nil, errors.New("密码至少 6 位")
	}
	if role == "" {
		role = "viewer"
	}
	hash, err := bcrypt.GenerateFromPassword([]byte(password), bcrypt.DefaultCost)
	if err != nil {
		return nil, err
	}
	u := &model.User{
		Username:     username,
		PasswordHash: string(hash),
		Role:         role,
		CreatedAt:    time.Now(),
	}
	if err := s.repo.CreateUser(u); err != nil {
		// 唯一约束冲突(sqlite error 2067)统一为"用户名已存在"
		return nil, errors.New("用户名已存在")
	}
	return u, nil
}

// Login 校验凭证并签发 JWT。
func (s *AuthService) Login(username, password string) (string, *model.User, error) {
	u, err := s.repo.GetByUsername(username)
	if err != nil {
		return "", nil, errors.New("用户名或密码错误")
	}
	if err := bcrypt.CompareHashAndPassword([]byte(u.PasswordHash), []byte(password)); err != nil {
		return "", nil, errors.New("用户名或密码错误")
	}
	token, err := s.sign(u)
	if err != nil {
		return "", nil, err
	}
	return token, u, nil
}

// ParseToken 校验并解析 JWT。
func (s *AuthService) ParseToken(token string) (*Claims, error) {
	claims := &Claims{}
	_, err := jwt.ParseWithClaims(token, claims, func(t *jwt.Token) (interface{}, error) {
		if _, ok := t.Method.(*jwt.SigningMethodHMAC); !ok {
			return nil, errors.New("非预期签名算法")
		}
		return []byte(s.jwtSecret), nil
	})
	if err != nil {
		return nil, err
	}
	return claims, nil
}

// SeedAdmin 表为空时插入 admin 账号,已存在则跳过(幂等)。
func (s *AuthService) SeedAdmin(user, pass string) error {
	if _, err := s.repo.GetByUsername(user); err == nil {
		return nil
	}
	_, err := s.Register(user, pass, "admin")
	return err
}

func (s *AuthService) sign(u *model.User) (string, error) {
	now := time.Now()
	claims := &Claims{
		UserID:   u.ID,
		Username: u.Username,
		Role:     u.Role,
		RegisteredClaims: jwt.RegisteredClaims{
			Subject:   itoa(u.ID),
			Issuer:    "insta360-insight",
			IssuedAt:  jwt.NewNumericDate(now),
			ExpiresAt: jwt.NewNumericDate(now.Add(7 * 24 * time.Hour)),
		},
	}
	tok := jwt.NewWithClaims(jwt.SigningMethodHS256, claims)
	return tok.SignedString([]byte(s.jwtSecret))
}

func itoa(n int64) string {
	if n == 0 {
		return "0"
	}
	neg := n < 0
	if neg {
		n = -n
	}
	buf := [20]byte{}
	i := len(buf)
	for n > 0 {
		i--
		buf[i] = byte('0' + n%10)
		n /= 10
	}
	if neg {
		i--
		buf[i] = '-'
	}
	return string(buf[i:])
}
```
- [ ] **Step 4: 运行测试确认通过**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/service/ -run 'TestRegister|TestLogin|TestParse|TestSeed' -v
```
Expected: 全部 PASS。
- [ ] **Step 5: 提交**
```bash
git add backend/internal/service/auth_service.go backend/internal/service/auth_service_test.go
git commit -m "feat(service): AuthService with bcrypt + JWT HS256"
```

---

### Task 5: handler 层 — Auth 端点（register / login）

**Files:**
- Create: `backend/internal/api/handler/auth.go`
- Test: `backend/internal/api/handler/auth_test.go`

**Interfaces:**
- Consumes: `*service.AuthService`
- Produces:
  - `func NewAuth(svc *service.AuthService) *Auth`
  - `func (h *Auth) Register(c *gin.Context)` — `POST /api/auth/register`
  - `func (h *Auth) Login(c *gin.Context)` — `POST /api/auth/login`

**响应约定:**
- 成功:`model.OK(data)`,HTTP 200。login 返回 `{ token, user }`;register 返回 `{ user }`。
- 参数缺失/业务失败:`model.Fail(code, msg)`,HTTP 200(业务码非 0)。
- `user` 在 JSON 中不含 `password_hash`(靠 `json:"-"`)。

- [ ] **Step 1: 写失败测试**（用 httptest + gin 引擎,直接挂 Auth 路由）
```go
// backend/internal/api/handler/auth_test.go
package handler

import (
	"bytes"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"

	"insta360-insight/internal/model"
	"insta360-insight/internal/service"
	"insta360-insight/internal/store"
)

func setupAuthEngine(t *testing.T) (*gin.Engine, *service.AuthService) {
	t.Helper()
	gin.SetMode(gin.TestMode)
	repo, err := store.NewUserRepo(t.TempDir() + "/h.db")
	if err != nil {
		t.Fatalf("repo: %v", err)
	}
	t.Cleanup(func() { repo.Close() })
	svc := service.NewAuthService(repo, "test-secret")
	r := gin.New()
	a := NewAuth(svc)
	r.POST("/api/auth/register", a.Register)
	r.POST("/api/auth/login", a.Login)
	return r, svc
}

func TestLoginSuccess(t *testing.T) {
	r, svc := setupAuthEngine(t)
	_, _ = svc.Register("u1", "secret123", "admin")

	body, _ := json.Marshal(map[string]string{"username": "u1", "password": "secret123"})
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/auth/login", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	r.ServeHTTP(w, req)

	if w.Code != http.StatusOK {
		t.Fatalf("expected 200, got %d", w.Code)
	}
	var resp model.APIResponse
	_ = json.Unmarshal(w.Body.Bytes(), &resp)
	if resp.Code != 0 {
		t.Fatalf("expected code 0, got %d msg=%s", resp.Code, resp.Message)
	}
	data := resp.Data.(map[string]interface{})
	if data["token"] == nil || data["token"] == "" {
		t.Fatal("expected token in data")
	}
	// 不应泄露 password_hash
	ub, _ := json.Marshal(data["user"])
	if bytes.Contains(ub, []byte("password_hash")) {
		t.Fatal("user JSON must not contain password_hash")
	}
}

func TestLoginWrongPassword(t *testing.T) {
	r, svc := setupAuthEngine(t)
	_, _ = svc.Register("u2", "secret123", "viewer")
	body, _ := json.Marshal(map[string]string{"username": "u2", "password": "bad"})
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/auth/login", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	r.ServeHTTP(w, req)
	var resp model.APIResponse
	_ = json.Unmarshal(w.Body.Bytes(), &resp)
	if resp.Code == 0 {
		t.Fatal("expected non-zero code for wrong password")
	}
}

func TestRegisterEndpoint(t *testing.T) {
	r, _ := setupAuthEngine(t)
	body, _ := json.Marshal(map[string]string{"username": "new", "password": "secret123"})
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodPost, "/api/auth/register", bytes.NewReader(body))
	req.Header.Set("Content-Type", "application/json")
	r.ServeHTTP(w, req)
	var resp model.APIResponse
	_ = json.Unmarshal(w.Body.Bytes(), &resp)
	if resp.Code != 0 {
		t.Fatalf("register failed: %s", resp.Message)
	}
}
```
- [ ] **Step 2: 运行测试确认失败**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/api/handler/ -run 'TestLogin|TestRegister' -v
```
Expected: 编译失败 `NewAuth undefined`。
- [ ] **Step 3: 写最小实现**

`backend/internal/api/handler/auth.go`:
```go
package handler

import (
	"net/http"

	"github.com/gin-gonic/gin"

	"insta360-insight/internal/model"
	"insta360-insight/internal/service"
)

// Auth 账号相关 HTTP 处理器(瘦层)。
type Auth struct {
	svc *service.AuthService
}

func NewAuth(svc *service.AuthService) *Auth { return &Auth{svc: svc} }

type authReq struct {
	Username string `json:"username"`
	Password string `json:"password"`
	Role     string `json:"role"`
}

// Register POST /api/auth/register
func (h *Auth) Register(c *gin.Context) {
	var q authReq
	if err := c.ShouldBindJSON(&q); err != nil || q.Username == "" || q.Password == "" {
		c.JSON(http.StatusOK, model.Fail(400, "用户名和密码必填"))
		return
	}
	u, err := h.svc.Register(q.Username, q.Password, q.Role)
	if err != nil {
		c.JSON(http.StatusOK, model.Fail(409, err.Error()))
		return
	}
	c.JSON(http.StatusOK, model.OK(gin.H{"user": u}))
}

// Login POST /api/auth/login
func (h *Auth) Login(c *gin.Context) {
	var q authReq
	if err := c.ShouldBindJSON(&q); err != nil || q.Username == "" || q.Password == "" {
		c.JSON(http.StatusOK, model.Fail(400, "用户名和密码必填"))
		return
	}
	token, u, err := h.svc.Login(q.Username, q.Password)
	if err != nil {
		c.JSON(http.StatusOK, model.Fail(401, "用户名或密码错误"))
		return
	}
	c.JSON(http.StatusOK, model.OK(gin.H{"token": token, "user": u}))
}
```
- [ ] **Step 4: 运行测试确认通过**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/api/handler/ -run 'TestLogin|TestRegister' -v
```
Expected: 全部 PASS。
- [ ] **Step 5: 提交**
```bash
git add backend/internal/api/handler/auth.go backend/internal/api/handler/auth_test.go
git commit -m "feat(handler): auth register/login endpoints"
```

---

### Task 6: middleware — JWTAuth 鉴权中间件

**Files:**
- Create: `backend/internal/middleware/auth.go`
- Test: `backend/internal/middleware/auth_test.go`

**Interfaces:**
- Consumes: `service.TokenValidator` 接口(`ParseToken(string) (*service.Claims, error)`);`AuthOptions{Disable bool}`
- Produces:
  - `type TokenValidator interface { ParseToken(token string) (*service.Claims, error) }`
  - `func JWTAuth(v TokenValidator, disable bool) gin.HandlerFunc`
  - 通过校验后 `c.Set("claims", *service.Claims)`;dev skip 时 `c.Set("claims", service.Claims{UserID:0, Username:"dev", Role:"admin"})`

**规则:**
- `Authorization: Bearer <token>` 缺失或格式错 → `c.AbortWithStatusJSON(401, model.Fail(401,"未提供有效令牌"))`。
- token 无效/过期 → `401` + `model.Fail(401,"令牌无效或已过期")`。
- `disable=true` → 直接注入 dev admin,`c.Next()`。

- [ ] **Step 1: 写失败测试**（用 fake validator 隔离,覆盖 4 种分支）
```go
// backend/internal/middleware/auth_test.go
package middleware

import (
	"net/http"
	"net/http/httptest"
	"testing"

	"github.com/gin-gonic/gin"

	"insta360-insight/internal/model"
	"insta360-insight/internal/service"
)

type fakeValidator struct {
	token string
	err   error
}

func (f fakeValidator) ParseToken(t string) (*service.Claims, error) {
	if f.err != nil {
		return nil, f.err
	}
	return &service.Claims{UserID: 1, Username: "u", Role: "admin"}, nil
}

func okHandler() gin.HandlerFunc {
	return func(c *gin.Context) { c.JSON(200, model.OK("ok")) }
}

func TestJWTAuth_ValidTokenPasses(t *testing.T) {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(JWTAuth(fakeValidator{token: "good"}, false))
	r.GET("/x", okHandler())
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodGet, "/x", nil)
	req.Header.Set("Authorization", "Bearer good")
	r.ServeHTTP(w, req)
	if w.Code != 200 {
		t.Fatalf("expected 200, got %d", w.Code)
	}
}

func TestJWTAuth_MissingTokenRejected(t *testing.T) {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(JWTAuth(fakeValidator{}, false))
	r.GET("/x", okHandler())
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodGet, "/x", nil)
	r.ServeHTTP(w, req)
	if w.Code != 401 {
		t.Fatalf("expected 401, got %d", w.Code)
	}
}

func TestJWTAuth_InvalidTokenRejected(t *testing.T) {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(JWTAuth(fakeValidator{err: ErrFake}, false))
	r.GET("/x", okHandler())
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodGet, "/x", nil)
	req.Header.Set("Authorization", "Bearer bad")
	r.ServeHTTP(w, req)
	if w.Code != 401 {
		t.Fatalf("expected 401, got %d", w.Code)
	}
}

func TestJWTAuth_DisableInjectsDevAdmin(t *testing.T) {
	gin.SetMode(gin.TestMode)
	r := gin.New()
	r.Use(JWTAuth(fakeValidator{err: ErrFake}, true))
	r.GET("/x", func(c *gin.Context) {
		cl := c.MustGet("claims").(service.Claims)
		c.JSON(200, model.OK(cl.Username))
	})
	w := httptest.NewRecorder()
	req := httptest.NewRequest(http.MethodGet, "/x", nil)
	r.ServeHTTP(w, req)
	if w.Code != 200 {
		t.Fatalf("expected 200, got %d", w.Code)
	}
}

var ErrFake = errors.New("fake")
```
- [ ] **Step 2: 运行测试确认失败**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/middleware/ -run TestJWTAuth -v
```
Expected: 编译失败 `JWTAuth undefined`。
- [ ] **Step 3: 写最小实现**

`backend/internal/middleware/auth.go`:
```go
package middleware

import (
	"errors"
	"net/http"
	"strings"

	"github.com/gin-gonic/gin"

	"insta360-insight/internal/model"
	"insta360-insight/internal/service"
)

// TokenValidator 是 JWTAuth 依赖的最小接口,便于测试与解耦。
type TokenValidator interface {
	ParseToken(token string) (*service.Claims, error)
}

// JWTAuth 解析 Bearer token 并校验;disable 时注入 dev admin(本地调试)。
func JWTAuth(v TokenValidator, disable bool) gin.HandlerFunc {
	return func(c *gin.Context) {
		if disable {
			c.Set("claims", service.Claims{
				UserID:   0,
				Username: "dev",
				Role:     "admin",
			})
			c.Next()
			return
		}
		header := c.GetHeader("Authorization")
		if !strings.HasPrefix(header, "Bearer ") {
			c.AbortWithStatusJSON(http.StatusUnauthorized, model.Fail(401, "未提供有效令牌"))
			return
		}
		claims, err := v.ParseToken(strings.TrimPrefix(header, "Bearer "))
		if err != nil {
			c.AbortWithStatusJSON(http.StatusUnauthorized, model.Fail(401, "令牌无效或已过期"))
			return
		}
		c.Set("claims", *claims)
		c.Next()
	}
}

// 让测试里的 ErrFake 有归属(实际未使用,保留以满足编译)
var _ = errors.New
```
> 说明:测试文件里 `ErrFake` 是 `errors.New` 的值,真实实现不需要它;上面 `var _ = errors.New` 仅为保留 import。你可以把 `errors` import 删掉,同时删掉那行占位,保持整洁。

- [ ] **Step 4: 运行测试确认通过**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./internal/middleware/ -run TestJWTAuth -v
```
Expected: 全部 PASS。
- [ ] **Step 5: 提交**
```bash
git add backend/internal/middleware/auth.go backend/internal/middleware/auth_test.go
git commit -m "feat(middleware): JWTAuth guard with dev-skip"
```

---

### Task 7: 接线 — main.go 初始化 auth + router 挂路由与中间件

**Files:**
- Modify: `backend/internal/api/router/router.go`（签名加 `authSvc *service.AuthService, disableAuth bool`；注册 `/api/auth/*`；受保护组挂 `middleware.JWTAuth`）
- Modify: `backend/main.go`（读 env、建 repo、SeedAdmin、构造 AuthService、传参给 router、全局挂载 middleware）

**Interfaces:**
- Consumes: `service.NewAuthService(repo, secret)`、`middleware.JWTAuth(svc, disable)`、`handler.NewAuth(svc)`
- Produces: 可运行的服务,`/api/auth/login` 公开,`/api/*` 数据接口受保护

- [ ] **Step 1: 修改 router.go**

新签名与路由（保留原有 insight/ai 路由,新增 auth + 保护组）:
```go
package router

import (
	"github.com/gin-gonic/gin"

	"insta360-insight/internal/api/handler"
	"insta360-insight/internal/middleware"
	"insta360-insight/internal/service"
)

// New 构造完整 *gin.Engine。
// authSvc 用于挂载登录/注册端点与 JWT 中间件;disableAuth 控制是否跳过鉴权。
func New(insightSvc *service.InsightService, aiSvc *service.AIService, authSvc *service.AuthService, disableAuth bool) *gin.Engine {
	r := gin.New()
	r.Use(gin.Recovery())

	health := handler.NewHealth()
	insight := handler.NewInsight(insightSvc, aiSvc)
	auth := handler.NewAuth(authSvc)

	// 健康检查 + 登录/注册:公开
	r.GET("/api/health", health.Handle)
	r.POST("/api/auth/register", auth.Register)
	r.POST("/api/auth/login", auth.Login)

	// 数据洞察组:受 JWTAuth 保护(health 已在外面,不受影响)
	g := r.Group("/api")
	g.Use(middleware.JWTAuth(authSvc, disableAuth))
	{
		g.GET("/kpi", insight.Kpi)
		g.GET("/views-trend", insight.ViewsTrend)
		g.GET("/platform-distribution", insight.PlatformShare)
		g.GET("/track-performance", insight.TrackPerformance)
		g.GET("/explosive-radar", insight.Radar)
		g.GET("/audience-age", insight.AudienceAge)
		g.GET("/top-creators", insight.TopCreators)
		g.GET("/insights", insight.Insights)
		g.GET("/filters/options", insight.FilterOptions)
	}

	return r
}
```
> 注意:原来 insight 路由在 `r.Group("/api")` 无中间件;现在统一移入受保护组 `g`,`/api/health` 与 `/api/auth/*` 留在组外公开。

- [ ] **Step 2: 修改 main.go**

在顶部 import 增加 `"os"`、`"log"`(已有)、`"insta360-insight/internal/store"`、`"insta360-insight/internal/middleware"`(router 已用)、`"insta360-insight/internal/service"`(已有)。`main()` 在初始化 dataset 之后插入:
```go
	// 5. 账号与鉴权(阶段一)
	jwtSecret := os.Getenv("JWT_SECRET")
	if jwtSecret == "" {
		jwtSecret = "dev-only-insecure-secret-change-me"
		log.Println("[WARN] JWT_SECRET 未设置,使用开发默认值;生产环境必须设置强随机值")
	}
	disableAuth := os.Getenv("AUTH_DISABLE") == "1" || os.Getenv("ENV") == "dev"

	dbPath := os.Getenv("DB_PATH")
	if dbPath == "" {
		dbPath = "data/app.db"
	}
	userRepo, err := store.NewUserRepo(dbPath)
	if err != nil {
		log.Fatalf("初始化用户库失败: %v", err)
	}
	defer userRepo.Close()

	authSvc := service.NewAuthService(userRepo, jwtSecret)
	seedUser := envOrDefault("SEED_ADMIN_USER", "admin")
	seedPass := envOrDefault("SEED_ADMIN_PASS", "insta360")
	if err := authSvc.SeedAdmin(seedUser, seedPass); err != nil {
		log.Fatalf("种子管理员失败: %v", err)
	}

	// 6. 注册路由 & 全局中间件(鉴权在 router 内部按组挂载)
	engine := router.New(insightSvc, aiSvc, authSvc, disableAuth)
	engine.Use(middleware.CORS(), middleware.AccessLog())
```
并新增辅助函数:
```go
func envOrDefault(key, def string) string {
	if v := os.Getenv(key); v != "" {
		return v
	}
	return def
}
```
> `engine.Use(middleware.CORS(), middleware.AccessLog())` 原本已存在,保留即可;注意 `JWTAuth` 已在 router 内按组挂载,不要在这里全局再挂一次(否则连 /api/auth/login 也被拦)。

- [ ] **Step 3: 编译并冒烟测试(无 AUTH_DISABLE)**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go build ./...
CGO_ENABLED=0 go run main.go &
sleep 3
# 无 token 直调受保护接口 -> 期望 401
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/kpi
# 登录 -> 期望返回 token
curl -s -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"insta360"}'
```
Expected: `kpi` 返回 `401`;login 返回 `{"code":0,"data":{"token":"...","user":{...}}}`。
- [ ] **Step 4: 提交**
```bash
git add backend/main.go backend/internal/api/router/router.go
git commit -m "feat(wire): init auth store/service, mount JWTAuth on data group"
```

---

### Task 8: 前端 — Pinia auth store

**Files:**
- Create: `frontend/src/stores/auth.js`

**Interfaces:**
- Produces: `useAuthStore`(state: `token`,`user`; getters: `isLoggedIn`; actions: `login(username,password)`,`logout()`)
- 持久化: `localStorage` key `insta_token` / `insta_user`
- 依赖:`api/request.js` 导出的 `login()`(Task 9 提供)

- [ ] **Step 1: 创建 store**
```js
// frontend/src/stores/auth.js
import { defineStore } from 'pinia'
import { login as apiLogin } from '@/api/request'

const TOKEN_KEY = 'insta_token'
const USER_KEY = 'insta_user'

function loadToken() {
  return localStorage.getItem(TOKEN_KEY) || ''
}
function loadUser() {
  const raw = localStorage.getItem(USER_KEY)
  if (!raw) return null
  try { return JSON.parse(raw) } catch { return null }
}

export const useAuthStore = defineStore('auth', {
  state: () => ({
    token: loadToken(),
    user: loadUser()
  }),
  getters: {
    isLoggedIn: (s) => !!s.token
  },
  actions: {
    async login(username, password) {
      const res = await apiLogin(username, password)
      // request.js 响应拦截器已把原始 APIResponse 透传(data 字段即响应体)
      if (!res || res.code !== 0) {
        throw new Error((res && res.message) || '登录失败')
      }
      this.token = res.data.token
      this.user = res.data.user
      localStorage.setItem(TOKEN_KEY, this.token)
      localStorage.setItem(USER_KEY, JSON.stringify(this.user))
    },
    logout() {
      this.token = ''
      this.user = null
      localStorage.removeItem(TOKEN_KEY)
      localStorage.removeItem(USER_KEY)
    }
  }
})
```
- [ ] **Step 2: 构建验证**
```bash
cd F:\workbuddy\影石\frontend
npm run build
```
Expected: 构建成功（若 `@` 别名报错,确认 `vite.config.js`/`jsconfig` 已配 `@`→`src`;本项目 main.js 用相对路径,若未配别名需改用相对路径 `../api/request`）。
- [ ] **Step 3: 提交**
```bash
git add frontend/src/stores/auth.js
git commit -m "feat(frontend): pinia auth store with localStorage persistence"
```

---

### Task 9: 前端 — request.js 增强（login 函数 + 401 跳登录）

**Files:**
- Modify: `frontend/src/api/request.js`

**Interfaces:**
- Consumes: 现有 axios 实例
- Produces: `export function login(username, password)`;响应拦截器 error 分支处理 HTTP 401 → 清 token + 跳 `#/login`

- [ ] **Step 1: 修改 request.js**

在文件末尾(`export default request` 之前)导出 login:
```js
// 登录:POST /api/auth/login,返回原始 APIResponse(含 code/data)
export function login(username, password) {
  return request.post('/auth/login', { username, password })
}
```
把响应拦截器 error 分支改为:
```js
  (error) => {
    // HTTP 401(后端 JWTAuth 拦截) -> 清 token 并跳登录页
    if (error.response && error.response.status === 401) {
      localStorage.removeItem('insta_token')
      localStorage.removeItem('insta_user')
      if (window.location.hash !== '#/login') {
        window.location.hash = '#/login'
      }
    }
    console.error('[request error]', error)
    return Promise.reject(error)
  }
```
> 说明:现有 success 分支已把原始响应体(`{code,data,message}`)透传给调用方,故 store 的 `res.code` 判断成立。

- [ ] **Step 2: 构建验证**
```bash
cd F:\workbuddy\影石\frontend
npm run build
```
Expected: 构建成功。
- [ ] **Step 3: 提交**
```bash
git add frontend/src/api/request.js
git commit -m "feat(frontend): add login api + 401 auto-redirect to login"
```

---

### Task 10: 前端 — LoginView 页面 + /login 路由

**Files:**
- Create: `frontend/src/views/LoginView.vue`
- Modify: `frontend/src/router/index.js`（新增 `/login` 路由,component 为 LoginView）

**Interfaces:**
- Consumes: `useAuthStore`(Task 8)、`useRouter`(vue-router)
- Produces: 登录页;提交后 `auth.login()` → 成功 `router.push('/insight')`;失败 `ElMessage` 提示

- [ ] **Step 1: 创建 LoginView.vue**（玻璃拟态 + 品牌色,与看板一致）
```vue
<template>
  <div class="login-stage">
    <div class="login-card">
      <div class="brand">
        <Insta360Logo />
        <h1>Insta360 达人营销洞察</h1>
        <p class="sub">数据洞察平台 · 请登录后继续</p>
      </div>
      <el-form @submit.prevent="onSubmit" label-position="top">
        <el-form-item label="账号">
          <el-input v-model="username" placeholder="请输入账号" size="large" />
        </el-form-item>
        <el-form-item label="密码">
          <el-input v-model="password" type="password" show-password placeholder="请输入密码" size="large" @keyup.enter="onSubmit" />
        </el-form-item>
        <el-button type="primary" size="large" class="submit" :loading="loading" @click="onSubmit">
          登录
        </el-button>
        <p v-if="err" class="err">{{ err }}</p>
      </el-form>
    </div>
  </div>
</template>

<script setup>
import { ref } from 'vue'
import { useRouter } from 'vue-router'
import { ElMessage } from 'element-plus'
import { useAuthStore } from '@/stores/auth'
import Insta360Logo from '@/components/Insta360Logo.vue'

const router = useRouter()
const auth = useAuthStore()
const username = ref('')
const password = ref('')
const loading = ref(false)
const err = ref('')

async function onSubmit() {
  err.value = ''
  if (!username.value || !password.value) {
    err.value = '请输入账号和密码'
    return
  }
  loading.value = true
  try {
    await auth.login(username.value, password.value)
    ElMessage.success('登录成功')
    router.push('/insight')
  } catch (e) {
    err.value = (e && e.message) || '登录失败'
  } finally {
    loading.value = false
  }
}
</script>

<style scoped lang="scss">
.login-stage {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  background: radial-gradient(1200px 600px at 50% -10%, #16203c 0%, #0b1020 60%);
}
.login-card {
  width: 380px;
  padding: 36px 32px;
  border-radius: 16px;
  background: rgba(19, 26, 48, 0.72);
  border: 1px solid rgba(61, 217, 235, 0.18);
  box-shadow: 0 20px 60px rgba(0, 0, 0, 0.45);
  backdrop-filter: blur(14px);
}
.brand { text-align: center; margin-bottom: 24px; }
.brand h1 { font-size: 18px; margin: 12px 0 4px; color: #eaf2ff; }
.brand .sub { font-size: 13px; color: #8fa3c8; margin: 0; }
.submit { width: 100%; margin-top: 8px; }
.err { color: #ff6b6b; font-size: 13px; text-align: center; margin: 12px 0 0; }
</style>
```
> 若项目未配 `@` 别名,将 `@/stores/auth`、`@/components/Insta360Logo.vue` 改为相对路径 `../stores/auth`、`../components/Insta360Logo.vue`。

- [ ] **Step 2: 修改 router/index.js 增加 /login**
在 `routes` 数组开头加:
```js
  { path: '/login', component: () => import('../views/LoginView.vue'), meta: { public: true, title: '登录' } },
```
其余路由保持不变。

- [ ] **Step 3: 构建验证**
```bash
cd F:\workbuddy\影石\frontend
npm run build
```
Expected: 构建成功,无 `LoginView`/`Insta360Logo` 解析错误。
- [ ] **Step 4: 提交**
```bash
git add frontend/src/views/LoginView.vue frontend/src/router/index.js
git commit -m "feat(frontend): LoginView page + /login route"
```

---

### Task 11: 前端 — 路由守卫（未登录跳登录 / 已登录访问登录页跳首页）

**Files:**
- Modify: `frontend/src/router/index.js`（加 `router.beforeEach` 全局前置守卫）
- Modify: `frontend/src/main.js`（无需改动;Pinia 已注册,guard 在 router 内读 localStorage 即可）

**Interfaces:**
- Consumes: `localStorage` 的 `insta_token`;`import.meta.env.VITE_AUTH_DISABLE`(dev 免登录开关)
- Produces: 受保护路由无 token → 重定向 `/login`;已登录访问 `/login` → 重定向 `/insight`

- [ ] **Step 1: 在 router/index.js 末尾(`export default router` 之前)加守卫**
```js
// 全局前置守卫:未登录跳登录页;已登录访问登录页跳首页
const AUTH_DISABLE = import.meta.env.VITE_AUTH_DISABLE === 'true'
router.beforeEach((to) => {
  if (AUTH_DISABLE) return true
  const token = localStorage.getItem('insta_token')
  const isPublic = to.meta && to.meta.public
  if (!token && !isPublic) {
    return '/login'
  }
  if (token && isPublic) {
    return '/insight'
  }
  return true
})
```
- [ ] **Step 2: 加 dev 免登录开关**

`frontend/.env.development`（新建）:
```
VITE_AUTH_DISABLE=true
```
> 说明:仅 `vite` dev server 与 `import.meta.env` 在 development 模式读取 `.env.development`;`vite build` 生产构建不读此文件,因此线上默认仍需登录,安全。

- [ ] **Step 3: 构建验证**
```bash
cd F:\workbuddy\影石\frontend
npm run build
```
Expected: 构建成功。
- [ ] **Step 4: 提交**
```bash
git add frontend/src/router/index.js frontend/.env.development
git commit -m "feat(frontend): route guard + dev auth-disable switch"
```

---

### Task 12: 端到端验收（对应 spec §3.3）

**Files:** 无新增,验证整体

**验收标准（全部必须满足）:**
1. 启动后端（无 AUTH_DISABLE）+ 前端 dev,浏览器访问 `/` → 重定向 `#/login`。
2. 用种子 `admin / insta360` 登录 → 进入看板,数据正常渲染。
3. 无 token 直调 `curl http://localhost:8080/api/kpi` → 返回 **HTTP 401**。
4. `AUTH_DISABLE=1` 启动后端 + 前端 `.env.development`(已含 `VITE_AUTH_DISABLE=true`) → 免登录直接进看板。
5. 库内仅存 bcrypt 哈希:直接 `sqlite` 查看 `users` 表,`password_hash` 为 `$2a$...` 开头,无明文。

- [ ] **Step 1: 启动后端（生产模式,无 skip）并验证 401 + 登录**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go run main.go &
sleep 3
echo "--- 无 token 调 kpi(期望 401) ---"
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/kpi
echo "--- 登录(期望返回 token) ---"
curl -s -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"insta360"}'
```
- [ ] **Step 2: 用拿到的 token 调受保护接口（期望 200 + 数据）**
```bash
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"insta360"}' | CGO_ENABLED=0 go run - <<'EOF'
// 仅示意:实际用 jq 解析更稳
EOF
)
# 推荐用 jq(若已装):
TOKEN=$(curl -s -X POST http://localhost:8080/api/auth/login -H "Content-Type: application/json" -d '{"username":"admin","password":"insta360"}' | jq -r .data.token)
curl -s -H "Authorization: Bearer $TOKEN" http://localhost:8080/api/kpi | head -c 200
```
- [ ] **Step 3: dev 放行模式验证**
```bash
cd F:\workbuddy\影石\backend
AUTH_DISABLE=1 CGO_ENABLED=0 go run main.go &
sleep 3
curl -s -o /dev/null -w "dev-skip kpi http=%{http_code}\n" http://localhost:8080/api/kpi
```
Expected: `dev-skip kpi http=200`。
- [ ] **Step 4: 前端冒烟（用 Edge 无头截图或手动浏览器）**
```bash
cd F:\workbuddy\影石/frontend
npm run dev &
# 浏览器开 http://localhost:5173/ -> 应跳 #/login
# 用 admin/insta360 登录 -> 进入 #/insight 看板渲染正常
```
- [ ] **Step 5: 运行全部后端测试做回归**
```bash
cd F:\workbuddy\影石\backend
CGO_ENABLED=0 go test ./... -v
```
Expected: 所有 task 的测试 PASS。
- [ ] **Step 6: 提交收尾（如有遗漏文件）**
```bash
git add -A
git commit -m "chore: phase-1 auth end-to-end verified"
```

---

## Self-Review（写完后自检）

**1. Spec 覆盖（spec §3 阶段一）**
- §3.1.1 技术选型（JWT HS256 / bcrypt / SQLite 纯 Go / 无 CGO）→ Task 1/4/3 ✅
- §3.1.2 `model.User` → Task 2 ✅
- §3.1.3 `store.UserRepo` 建表/增/查/种子 → Task 3 ✅（SeedAdmin 实现在 Task 4,依赖 repo.Count/GetByUsername）
- §3.1.4 `service.AuthService` Register/Login/ParseToken → Task 4 ✅
- §3.1.5 `handler.Auth` register/login → Task 5 ✅
- §3.1.6 `middleware.JWTAuth` 保护组 + dev skip → Task 6 ✅
- §3.2.1~3.2.4 前端 LoginView/store/守卫/拦截器 → Task 8/9/10/11 ✅
- §3.2.5 看板零改动 → 全程未碰看板组件 ✅
- §3.3 验收 → Task 12 ✅

**2. Placeholder 扫描**:无 TBD/TODO/“add validation later”。每个代码 step 均给出完整实现。前端测试以构建+浏览器验证替代（已在任务开头说明理由,非 placeholder）。

**3. 类型一致性**:
- `service.Claims` 在 Task 4 定义,Task 5/6 复用 `service.Claims` 类型名与字段（`UserID/Username/Role`）一致 ✅
- `middleware.TokenValidator` 接口方法签名 `ParseToken(string) (*service.Claims, error)` 与 `service.AuthService.ParseToken` 完全一致 ✅
- `router.New` 新签名 `(insightSvc, aiSvc, authSvc, disableAuth)` 在 Task 7 同时改 router.go 与 main.go,一致 ✅
- `model.Fail` 在 Task 2 定义,Task 5/6 调用一致 ✅
- 前端 `res.code / res.data.token / res.data.user` 与后端 login 响应 `{code:0,data:{token,user}}` 一致 ✅

**风险与注意**:
- `modernc.org/sqlite` 首次 `go mod tidy` 会拉较多间接依赖;若本机 `proxy.golang.org` 不可达,需在放行网络下执行 Task 1（已在 step 1 注明）。
- 前端若未配 `@` 别名,Task 8/10 需改用相对路径（已在对应 step 注明）。
- `data/app.db` 在 backend 工作目录下生成;CloudStudio 部署后端时需确保该目录可写或改 `DB_PATH`。
