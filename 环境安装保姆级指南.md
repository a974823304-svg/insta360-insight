# 环境安装保姆级指南（Insta360 达人营销数据洞察平台）

读完这份，你只需要手动点几下鼠标装**两样东西**，项目就能前后端一起跑起来。

> **为什么是我来装而不是工具帮你装？**
> 这两样的官方下载源（Go 在 `storage.googleapis.com`、VC++ 在微软 CDN）被你电脑当前的网络代理拦截了，工具在本环境里下不下来。但它们都是**官方、标准、安全**的组件，手动装一次即可，装好之后项目就能正常运行。

---

## 你只需要装两样

| # | 组件 | 给谁用 | 不装会怎样 |
|---|------|--------|-----------|
| 1 | **Go 语言** | 后端（Gin） | 后端跑不起来 |
| 2 | **Visual C++ 2015–2022 运行库 (x64)** | 前端（Vite/rollup/esbuild 需要） | 前端 `npm run dev` 报 `rollup ... is not a valid Win32 application` |

---

## 一、安装 Go（后端运行环境）

### 方式 A：winget 一行命令（推荐，最快）
1. 打开「终端」：
   - 按 `Win` 键 → 输入 `PowerShell` → 右键「**以管理员身份运行**」。
2. 粘贴回车：
   ```powershell
   winget install GoLang.Go
   ```
   - 中途若让你确认协议，输入 `Y` 回车。
   - 出现「已成功安装」即可。
3. **关键一步**：把这个终端窗口**关掉**，重新打开一个（让系统 PATH 刷新）。
4. 验证：在新终端输入
   ```powershell
   go version
   ```
   出现 `go version go1.xx.x windows/amd64` 就成功了 ✅

### 方式 B：手动下载安装包（winget 用不了时）
1. 浏览器打开 https://go.dev/dl/
2. 找到 **Windows** 那一行，点 `go1.xx.x.windows-amd64.msi` 下载。
3. 双击 `.msi` → 一路「Next」→「Install」→ 完成。
4. 重启终端，执行 `go version` 验证。

### 装好后跑后端
```powershell
cd F:\workbuddy\影石\backend
go mod tidy      # 首次会自动拉取 Gin 依赖（走 proxy.golang.org，可用）
go run main.go
```
浏览器访问 http://localhost:8080/api/health ，看到 `{"status":"ok",...}` 即成功 ✅

---

## 二、安装 VC++ 运行库 x64（前端必装）

> 不装这个，前端 `npm run dev` 一定会失败（报错就是缺这个运行库）。注意必须是 **x64** 版，因为你的 Node 是 64 位的。

1. 浏览器打开下载地址（任选其一）：
   - 短链：https://aka.ms/vs/17/release/vc_redist.x64.exe
   - 或搜索「Visual C++ 2015-2022 Redistributable x64」进微软官网。
2. 双击 `vc_redist.x64.exe` → 勾选「我同意」→「安装」。
   - 若弹窗要管理员权限，点「是」。
3. 安装完**重启电脑**（或至少重启终端）。
4. 验证：直接回前端目录试跑：
   ```powershell
   cd F:\workbuddy\影石\frontend
   npm run dev
   ```
   看到 `Local:   http://localhost:5173/` 就成功了 ✅，浏览器打开该地址即可。

---

## 三、前后端一起跑（完整体验）

开**两个**终端窗口：
- 终端 1：`cd F:\workbuddy\影石\backend` → `go run main.go`
- 终端 2：`cd F:\workbuddy\影石\frontend` → `npm run dev`

前端页面里的 KPI 卡片会通过 `/api` 代理自动调用后端接口（端口 8080）。

---

## 四、常见问题自查（保姆级排错）

- **`go : 无法将“go”识别为命令`**
  → 没重启终端，或没装成功。先关掉终端重开；还不行就重装一遍。

- **装 Go 时网络报错（connect / 403）**
  → 你电脑的代理很可能也拦了 go.dev。改用「方式 B 手动下载」；如果浏览器也下不动，就换一个不拦截这些域名的网络（或让公司 IT 提供内网镜像）再装。

- **`npm run dev` 仍报 rollup / esbuild 错误**
  → VC++ 没装，或装成了 x86 版。确认装的是 **x64** 版且**已重启电脑**。

- **`go mod tidy` 卡住 / 报错**
  → 公司代理若拦了 `proxy.golang.org`，依赖也拉不下来，需换网络。

---

## 五、验证清单（全绿 = 大功告成）

- [ ] `go version` 有输出版本号
- [ ] 后端 `go run main.go` 后，http://localhost:8080/api/health 返回 JSON
- [ ] `npm run dev` 正常启动，无 rollup 报错
- [ ] 浏览器打开 http://localhost:5173 能看到页面和 KPI 卡片

---

## 附：本项目结构
```
F:\workbuddy\影石\
├── README.md                  # 项目总说明
├── 环境安装保姆级指南.md        # 本文件
├── backend/                   # Go 后端（Gin）
├── frontend/                  # Vue3 前端（依赖已装好）
└── ai/                        # Python AI 引擎依赖占位
```
